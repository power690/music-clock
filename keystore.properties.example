# 签名配置示例文件
#
# 使用方法：
# 1. 复制本文件为根目录下的 local.properties（注意：local.properties 已在 .gitignore 中，不会被提交）
# 2. 把你自己的签名信息填进去
# 3. 重新构建即可使用你的签名
#
# 注意：本仓库不包含任何签名文件，你需要自己生成或导入。
#   生成新签名：keytool -genkey -v -keystore release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias release
#
# 在 local.properties 中填写以下内容（示例）：
#
#   storeFile=../keystore/release.jks
#   storePassword=你的密码
#   keyAlias=release
#   keyPassword=你的密码
#
# 或通过环境变量传入：
#
#   export STORE_FILE=../keystore/release.jks
#   export STORE_PASSWORD=你的密码
#   export KEY_ALIAS=release
#   export KEY_PASSWORD=你的密码
